/**
 * Legacy background component. The app now uses a global
 * `<UniverseBackground />` mounted in App.tsx, so this component
 * is intentionally a no-op to let the 3D universe show through.
 */
export default function CosmicBackground() {
  return null;
}
